services:
  - type: web
    name: cymorallvideodownloader
    env: node
    plan: free
    buildCommand: "apt-get update && apt-get install -y ffmpeg python3 && npm install"
    startCommand: "npm start"
