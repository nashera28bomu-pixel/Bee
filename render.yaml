services:
  - type: web
    name: cymorallvideodownloader
    env: node
    plan: free

    buildCommand: |
      npm install
      pip install yt-dlp

    startCommand: npm start

    envVars:
      - key: NODE_VERSION
        value: 20
